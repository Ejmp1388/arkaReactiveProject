name: Deploy Report Low Stock Service

on:
  push:
    branches: [main]
    paths: ['reporte-service/**']

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: reporte-service

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Make mvnw executable
      run: chmod +x mvnw

    - name: Build JAR
      run: ./mvnw clean package -DskipTests

    - name: Set version
      id: version
      run: echo "VERSION=$(date +%Y%m%d)-${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

    - name: Log in to Docker Hub
      run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

    - name: Build Docker image
      run: |
        docker build --build-arg JAR_FILE=target/reporte-service-*.jar \
          -t ${{ secrets.DOCKER_USERNAME }}/reporte-service:${{ steps.version.outputs.VERSION }} \
          -t ${{ secrets.DOCKER_USERNAME }}/reporte-service:latest .

    - name: Push Docker image
      run: |
        docker push ${{ secrets.DOCKER_USERNAME }}/reporte-service:${{ steps.version.outputs.VERSION }}
        docker push ${{ secrets.DOCKER_USERNAME }}/reporte-service:latest

    - name: Deploy to EC2
      uses: appleboy/ssh-action@v0.1.10
      with:
        host: ${{ secrets.EC2_HOST_2 }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.EC2_KEY }}
        script: |
          docker pull ${{ secrets.DOCKER_USERNAME }}/reporte-service:latest
          docker stop reporte-service || true
          docker rm reporte-service || true
          docker run -d --name reporte-service -e ACCESS_KEY=${{ secrets.ACCESS_KEY_S }} -e SECRET_KEY=${{ secrets.SECRET_KEY_S }} -p 8090:8090 ${{ secrets.DOCKER_USERNAME }}/reporte-service:latest
