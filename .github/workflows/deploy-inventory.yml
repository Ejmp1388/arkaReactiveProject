name: Deploy Inventory Service

on:
  push:
    branches: [main]
    paths: ['inventory-service/**']

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: inventory-service

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Make mvnw executable
      run: chmod +x mvnw

    - name: Build JAR
      run: ./mvnw clean package -DskipTests

    - name: Set version
      id: version
      run: echo "VERSION=$(date +%Y%m%d)-${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

    - name: Log in to Docker Hub
      run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

    - name: Build Docker image
      run: |
        docker build --build-arg JAR_FILE=target/inventory-service-*.jar \
          -t ${{ secrets.DOCKER_USERNAME }}/inventory-service:${{ steps.version.outputs.VERSION }} \
          -t ${{ secrets.DOCKER_USERNAME }}/inventory-service:latest .

    - name: Push Docker image
      run: |
        docker push ${{ secrets.DOCKER_USERNAME }}/inventory-service:${{ steps.version.outputs.VERSION }}
        docker push ${{ secrets.DOCKER_USERNAME }}/inventory-service:latest

    - name: Deploy to EC2
      uses: appleboy/ssh-action@v0.1.10
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.EC2_KEY }}
        script: |
          docker pull ${{ secrets.DOCKER_USERNAME }}/inventory-service:latest
          docker stop inventory-service || true
          docker rm inventory-service || true
          docker run -d --name inventory-service -p 8081:8080 ${{ secrets.DOCKER_USERNAME }}/inventory-service:latest
